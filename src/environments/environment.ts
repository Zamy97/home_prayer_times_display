export const environment = {
  production: false,
  /**
   * Optional: for local `ng serve` only. In production the key lives in
   * Vercel as OPENWEATHER_API_KEY and the app calls /api/weather.
   * Leave empty when using `vercel dev`.
   */
  openWeatherApiKey: '',
};
